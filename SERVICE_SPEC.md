# henro-log（遍路ログ） - サービス仕様書

## 1. サービス概要
- **一言で**: 四国八十八ヶ所を「少しずつ」巡る歩みを、訪問1件ずつのイベントとして記録し、同行者や前回からの間隔まで横断的に振り返れる巡拝記録アプリ。
- **対象ユーザー**: 区切り打ち（複数回に分けて巡る）でお遍路を回る人。まずは作者自身（個人用MVP）。
- **解決する課題**: 既存アプリは「札所に日付・写真を貼る」モデルで、"誰と行ったか""前回から何年ぶりか"といった文脈・関連性を構造化して振り返れない。henro-log は訪問をイベントとして記録し、同行者軸・再訪間隔で巡拝の物語を残す。
- **ステータス**: MVP

## 2. ユーザーロールと権限
| ロール | できること |
|---|---|
| 利用者（本人） | 訪問記録の作成・編集・閲覧、同行者マスタの管理、集計の閲覧。MVPは単一ユーザー・ローカル完結。 |

## 3. 機能一覧
### 実装済み
- [x] 札所一覧 — 88札所を訪問済/未訪問・最終訪問日・訪問回数つきで一覧表示（道場別・進捗バー）
- [x] 訪問記録の作成/編集/削除 — 札所・訪問日・同行者・天候・移動手段・心境メモ・納経有無を記録
- [x] タイムライン — 全訪問を時系列で表示
- [x] 同行者ビュー — 同行者ごとに「一緒に回った札所」を集計表示
- [x] 再訪間隔の算出 — 同一札所の訪問履歴から「前回から○年ぶり・○回目」を自動表示
- [x] 地図（主要札所） — デフォルメ四国SVGに主要15札所のピン。タップで札所カード→詳細。参拝済/未を塗り分け
- [x] PWA化 — インストール可能・オフライン（vite-plugin-pwa）
- [x] データのバックアップ/エクスポート — JSON エクスポート/インポート

### 未実装（予定）
- [ ] 地図のGPS連動 — 現在地から近い札所を表示（地図タブに導線のみ用意済み「準備中」）
- [ ] 地図の全88札所化 — temples の lat/lng 投入後、投影配置に置き換え
- [ ] 写真の保存 — 端末内に画像を保持（IndexedDB / Blob、photosテーブルは定義済み）
- [ ] クラウド同期 — 複数端末間の同期

## 4. 画面一覧
| 画面 | パス | 対象ロール | 概要 |
|---|---|---|---|
| 札所一覧 | `/` | 本人 | 88札所の進捗一覧。各札所の訪問状況・最終訪問日・回数 |
| 地図 | `/map` | 本人 | デフォルメ四国マップ。主要札所ピンをタップ→札所カード→詳細 |
| 札所詳細 | `/temple/:id` | 本人 | 1札所の情報＋その札所の訪問履歴（再訪間隔つき） |
| 訪問記録 作成/編集 | `/visit/new`, `/visit/:id` | 本人 | 訪問イベントの入力フォーム |
| タイムライン | `/timeline` | 本人 | 全訪問の時系列表示 |
| 同行者ビュー | `/companions` | 本人 | 同行者ごとの巡拝集計 |
| 設定 | `/settings` | 本人 | エクスポート/インポート等 |

## 5. データモデル
### コレクション / テーブル一覧（IndexedDB / Dexie）
- `temples` — 札所マスタ（88件固定・シードで内蔵）
  - 主要フィールド: id(番号1-88), name(寺名), sect(宗派), honzon(ご本尊), prefecture, address, lat, lng
- `visits` — 訪問記録（記録の中心。1札所につき何件でも）
  - 主要フィールド: id, templeId, visitedOn(訪問日), companionIds[], weather(天候), transport(移動手段), photoIds[], note(心境メモ), nokyo(納経有無 bool), createdAt, updatedAt
- `companions` — 同行者マスタ（集計のためマスタ化）
  - 主要フィールド: id, name, relation(関係: 家族/友人 等)
- `photos` — 写真（Blob 保管）
  - 主要フィールド: id, blob, mime, createdAt

### 派生（計算で導出）
- 札所の訪問回数 / 最終訪問日 … `visits` を templeId で集約
- 前回からの間隔・○回目 … 同一 templeId の `visits` を visitedOn 順に並べて差分
- 同行者ごとの集計 … `visits.companionIds` を companion で逆引き集約

## 6. 外部連携
- 認証: なし（MVPは単一ユーザー・ローカル完結）
- DB: IndexedDB（Dexie.js）。サーバレス
- API: なし（札所マスタは静的JSON同梱）
- ホスティング: 静的ホスティング（GitHub Pages / xserver 等。PWAとして配信）

## 7. ビジネスルール
- 1つの札所に対し訪問記録（visit）は複数作成できる（区切り打ち・お礼参り対応）
- 訪問日（visitedOn）は必須。同行者・天候・写真・メモは任意
- 「前回からの間隔」は同一札所の直近2件の visitedOn 差から算出
- 同行者は名前で重複させずマスタ参照する（集計の正確性のため）

## 8. 非機能要件
- 想定ユーザー数: 1（MVP）。将来的に個人ごとローカル完結の複数ユーザー
- パフォーマンス目標: 88札所一覧・数百件の訪問記録を即時表示
- セキュリティ: 個人データは端末内に保持。クラウド送信なし（MVP）
- オフライン: PWAでオフライン記録・閲覧が可能であること

## 9. 既知の課題・制限
- MVPはローカル完結のため、機種変更時はエクスポート/インポートが必要
- 複数端末間の同期は未対応（将来のクラウド同期で検討）
- 別格二十霊場・他霊場は対象外（四国八十八ヶ所のみ）

## 10. 更新履歴
| 日付 | 内容 |
|---|---|
| 2026-06-27 | 初版作成 |
