# henro-log（遍路ログ） - サービス仕様書

## 1. サービス概要
- **一言で**: 四国八十八ヶ所を「少しずつ」巡る歩みを、訪問1件ずつのイベントとして記録し、同行者や前回からの間隔まで横断的に振り返れる巡拝記録アプリ。
- **対象ユーザー**: 区切り打ち（複数回に分けて巡る）でお遍路を回る人。まずは作者自身（個人用MVP）。
- **解決する課題**: 既存アプリは「札所に日付・写真を貼る」モデルで、"誰と行ったか""前回から何年ぶりか"といった文脈・関連性を構造化して振り返れない。henro-log は訪問をイベントとして記録し、同行者軸・再訪間隔で巡拝の物語を残す。
- **ステータス**: MVP

## 2. ユーザーロールと権限
| ロール | できること |
|---|---|
| 利用者（本人） | 訪問記録の作成・編集・閲覧、同行者マスタの管理、集計の閲覧。MVPは単一ユーザー・ローカル完結。 |

## 3. 機能一覧
### 実装済み
- [x] 札所（縦スクロールの巡拝トレイル／押印） — アプリのホーム（`/`・「札所」タブ）＝88札所を**1つずつ縦に連ねた巡拝トレイル**（点線の巡拝路で連結、道場別に4区切り、長い分はスクロール）。各行に **番号＋寺名＋所在市・ご本尊＋最終参拝日/回数**（未参拝は「未参拝」）を表示。**行をタップすると確認シートが開き（未参拝＝「この札所に参拝を記録しますか？」＋記録する／キャンセル。誤タップ防止のため即記録はしない／参拝済＝今日また参拝・詳細写真へ・最新を取り消し）、記録するを押すと今日の日付で参拝を1件記録**し御朱印風「済」スタンプが押される（記録時にファミコン風の押印効果音）。確認シートには札所ごとの挿絵（お堂・三重塔・石灯籠・本堂の4種を番号で割当）を表示。複数回参拝は回数バッジ。上部の進捗は「○/88」の道の上を**お遍路さん（住職）が歩いて進むアニメーション**（2コマ＋上下バウンド、進捗％の位置まで前進）＋残数メッセージ＋％。**番号・「済」は御朱印/納経帳ふうの毛筆体**（Yuji Syuku をサブセット同梱＝約5KB・オフライン対応）で表示。**巡礼を楽しくする演出**＝巡拝路は一直線ではなく左右に蛇行しながら下る（サイン波・道場ごとに連続する滑らかな曲線、SVG）／背後に四国八十八か所の絵地図を薄く（opacity 0.12）敷く／スクロール中はヘッダー・フッターを透明にして背景の絵地図を見せる（停止で白に戻る）／歩いた分（現在地まで）が朱色に色づく／次に行く札所に「つぎ」パルス／道場ごとに進捗（○/N）と満願バッジ／節目（初参拝・道場満願・八十八結願）で花吹雪＋特別トースト／押印は手押しっぽい微回転
- [x] 訪問記録の作成/編集/削除 — 盤タップで今日の参拝を即記録。写真・心境メモ・納経有無つきの詳しい記録/編集は札所詳細→フォーム（`/visit/new?temple=`, `/visit/:id`）で行う（同行者/天候/移動手段は廃止）
- [x] 写真（カメラ） — 撮影/選択した写真を端末内（IndexedDB Blob）に保存。フォームでサムネイル/削除、札所詳細で表示
- [x] 写真の自動縮小 — 保存前に長辺1600px以内へ縮小しJPEG(品質0.82)で再エンコード。EXIF回転を吸収。再エンコードで増える場合は原本を保持（`src/lib/downscaleImage.ts`）。実測で3000×2000写真が約77%削減
- [x] 写真ビューア（記念フレーム） — サムネタップで全画面表示。「第○番 寺名・参拝日・朱印風"巡拝"」を添えた記念フレーム。スワイプ/矢印/Escで操作
- [x] 写真アルバム — 全札所の写真を時系列グリッドで一覧（/album タブ）。タップで全画面ビューアへ
- [x] 記念フレーム画像のシェア/保存 — ビューアから写真＋和紙フレーム（寺名・日付・朱印風）を1080x1350 PNGに合成し、Web Share APIで共有（非対応時DL）。SNS拡散による集客導線＝競合優位の核
- [x] タイムライン — 全訪問を時系列で表示
- [x] 再訪間隔の算出 — 同一札所の訪問履歴から「前回から○年ぶり・○回目」を自動表示
- [x] PWA化 — インストール可能・オフライン（vite-plugin-pwa）
- [x] プロフィール編集 — 名前・メールアドレスを端末内に保存（将来のユーザー認証の布石）
- [x] 音楽（BGM）ON/OFF — 設定でファミコン風の巡礼テーマBGMを ON/OFF。ON にすると全画面でループ再生（設定は localStorage に保存、自動再生ブロック時は次のタップで再生開始）

### 廃止
- 同行者・天候・移動手段の記録、同行者ビュー（2026-06-27 にユーザー判断で削除。companions テーブルは互換のため残置）
- 設定のJSONインポート/エクスポート（2026-06-27 に削除）
- 地図タブ `/map`（GPSで近くの札所を探す機能つき）（2026-07-03 に廃止。すごろく盤が地図を兼ねる方針に一本化。`MapView.tsx`/`mapPoints.ts`/`lib/geo.ts` を削除）
- すごろくタブ（2026-07-03 に廃止）。記録は「フッター＋＝すごろく盤」に一本化 →さらに同日、**「札所」タブ（ホーム `/`）自体をすごろく押印盤に統合**。道場別リスト表示（旧 `TempleList.tsx`）と ＋（参拝を記録）FAB、`/board` ルートを廃止。押印はホームの盤で行う

### 未実装（予定）
- [ ] クラウド同期 — 複数端末間の同期

## 4. 画面一覧
| 画面 | パス | 対象ロール | 概要 |
|---|---|---|---|
| 札所（巡拝トレイル／押印） | `/` | 本人 | ホーム＝88札所を1つずつ縦に連ねた巡拝トレイル（番号＋寺名＋所在/本尊＋最終日/回数、道場別4区切り、スクロール）。行をタップ→確認して今日の参拝を記録し御朱印風「済」スタンプ。参拝済みは確認シート（再記録／詳細／取り消し）。進捗○/88 |
| 札所詳細 | `/temple/:id` | 本人 | 1札所の情報＋その札所の訪問履歴（再訪間隔つき） |
| 訪問記録 作成/編集 | `/visit/new`, `/visit/:id` | 本人 | 訪問イベントの入力フォーム（札所・日付・写真・納経・メモ） |
| タイムライン | `/timeline` | 本人 | 全訪問の時系列表示 |
| 写真アルバム | `/album` | 本人 | 全写真を時系列グリッドで一覧→全画面ビューア |
| 設定 | `/settings` | 本人 | プロフィール（名前・メール）の編集、音楽（BGM）の ON/OFF |

## 5. データモデル
### コレクション / テーブル一覧（IndexedDB / Dexie）
- `temples` — 札所マスタ（88件固定・シードで内蔵）
  - 主要フィールド: id(番号1-88), name(寺名), sect(宗派), honzon(ご本尊), prefecture, city, lat, lng
  - lat/lng は全88件投入済み（Wikipedia座標, WGS84）。15/17/18/19番は誤り修正済み
- `visits` — 訪問記録（記録の中心。1札所につき何件でも）
  - 主要フィールド: id, templeId, visitedOn(訪問日), photoIds[], note(心境メモ), nokyo(納経有無 bool), createdAt, updatedAt
  - 廃止フィールド: companionIds[]（常に空配列で保存）, weather, transport（スキーマは残置）
- `photos` — 写真（Blob 保管）
  - 主要フィールド: id, blob, mime, createdAt
- `profile` — 利用者プロフィール（単一レコード id=1。Dexie version 2 で追加）
  - 主要フィールド: id(=1), name, email
- `companions` — 同行者マスタ（機能廃止。スキーマのみ残置）

### 派生（計算で導出）
- 札所の訪問回数 / 最終訪問日 … `visits` を templeId で集約
- 前回からの間隔・○回目 … 同一 templeId の `visits` を visitedOn 順に並べて差分

## 6. 外部連携
- 認証: なし（MVPは単一ユーザー・ローカル完結）
- DB: IndexedDB（Dexie.js）。サーバレス
- API: なし（札所マスタは静的JSON同梱）
- ホスティング: 静的ホスティング（GitHub Pages / xserver 等。PWAとして配信）

## 7. ビジネスルール
- 1つの札所に対し訪問記録（visit）は複数作成できる（区切り打ち・お礼参り対応）
- 訪問日（visitedOn）は必須。写真・メモ・納経は任意
- 「前回からの間隔」は同一札所の直近2件の visitedOn 差から算出

## 8. 非機能要件
- 想定ユーザー数: 1（MVP）。将来的に個人ごとローカル完結の複数ユーザー
- パフォーマンス目標: 88札所一覧・数百件の訪問記録を即時表示
- セキュリティ: 個人データは端末内に保持。クラウド送信なし（MVP）
- オフライン: PWAでオフライン記録・閲覧が可能であること

## 9. 既知の課題・制限
- MVPはローカル完結のため、機種変更時はエクスポート/インポートが必要
- 複数端末間の同期は未対応（将来のクラウド同期で検討）
- 別格二十霊場・他霊場は対象外（四国八十八ヶ所のみ）

## 10. 更新履歴
| 日付 | 内容 |
|---|---|
| 2026-06-27 | 初版作成 |
| 2026-07-01 | すごろく盤（スタンプ）画面 `/board` を追加。88札所を蛇行自動配置、参拝済みに押印。プロジェクトを `01_projects/private/` から `00_prototypes/` へ移動 |
| 2026-07-03 | 写真の自動縮小を追加（保存前に長辺1600px・JPEG再エンコード。EXIF回転吸収・肥大時は原本保持） |
| 2026-07-03 | 地図を全88札所化。実緯度経度を島の内側矩形へ線形投影して自動配置（手置き15点を廃止） |
| 2026-07-03 | すごろく盤を記録の主役に転換＝タップで今日の参拝を即記録／参拝済みは確認シート（再記録・詳細・取り消し）。地図タブ `/map` を廃止しすごろくに一本化 |
| 2026-07-03 | すごろく盤の札所番号・「済」スタンプを御朱印風の毛筆体に（Yuji Syuku をサブセット同梱・約5KB・OFL） |
| 2026-07-03 | 記録導線を一本化。すごろくタブを廃止し、フッター＋「参拝を記録」の遷移先をすごろく盤に変更（タップ→確認→記録）。タブは4つに |
| 2026-07-03 | 「札所」タブ（ホーム `/`）自体をすごろく押印盤に統合。道場別リスト（TempleList）と ＋FAB、`/board` ルートを廃止し、押印はホームの盤で行う形に |
| 2026-07-03 | 押印盤を「1画面詰め込みの蛇行盤」から「1札所ずつ縦に連ねた巡拝トレイル（寺名入り・道場別・スクロール）」に作り替え。蛇行座標データ `boardPath.ts` を廃止 |
| 2026-07-03 | トレイルに楽しさ演出を追加＝進捗メッセージ/％、歩いた道の色づき、次札所の「つぎ」パルス、道場別進捗＋満願バッジ、節目の花吹雪＋特別トースト、押印の微回転 |
| 2026-07-03 | 巡拝路を一直線→左右に蛇行して下る滑らかな曲線（SVG・サイン波）に変更。背後に四国のシルエットを薄く敷いた |
| 2026-07-03 | 背景をSVGシルエットから「四国八十八か所巡り」の絵地図（提供画像）に差し替え。長辺1600pxに最適化して同梱、opacity 0.12 |
| 2026-07-03 | スクロール中だけヘッダー・フッターを透明化し背景の絵地図を見せる（`useIsScrolling` フック、停止で白に戻る） |
| 2026-07-04 | 設定に音楽（BGM）ON/OFF を追加（ファミコン風テーマをループ再生）。上部の進捗バーを、お遍路さん（住職）が歩いて進むアニメーションに変更（2コマ・進捗位置まで前進） |
| 2026-07-04 | 記録時にファミコン風の押印効果音を追加（合成WAV）。確認シートに札所ごとの挿絵（4種・番号で割当）を表示 |
